# Iron Point Website

Next.js MVP for Iron Point, including public pages, a private AI system estimator, lead capture, and a simple admin area.

## Local Development

```bash
npm install
npm run db:init
npm run dev
```

The local admin password is `ironpoint-admin`. Change `ADMIN_PASSWORD` and `ADMIN_SECRET` before using the admin area outside local development.
